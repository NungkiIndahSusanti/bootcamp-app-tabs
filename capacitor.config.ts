import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.sekawanmedia.bootcampionictabs',
  appName: 'bootcamp -app -tabs',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
